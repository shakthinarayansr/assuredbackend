name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: assuredgig
          POSTGRES_PASSWORD: assuredgig
          POSTGRES_DB: assuredgig_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U assuredgig"
          --health-interval 5s --health-timeout 5s --health-retries 10
      redis:
        image: valkey/valkey:8-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 5s --health-timeout 5s --health-retries 10

    env:
      DATABASE_URL: postgresql://assuredgig:assuredgig@localhost:5432/assuredgig_test?schema=public
      REDIS_URL: redis://localhost:6379
      JWT_ACCESS_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      OTP_PEPPER: ci-pepper
      NODE_ENV: test

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - run: npx prisma generate

      - name: Lint
        run: npm run lint

      - name: Migration check
        run: npx prisma migrate deploy

      - name: Unit tests
        run: npm test

      - name: E2E tests
        run: npm run test:e2e

      - name: Build
        run: npm run build

      # Contract: an OpenAPI change that breaks the generated Flutter client
      # must fail CI (TRD §15). Wire this up once the spec is committed.
      # - name: OpenAPI diff
      #   run: npx oasdiff breaking openapi.baseline.json openapi.json --fail-on ERR
